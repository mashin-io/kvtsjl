[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kvtsjl"
dynamic = ["version"]
description = "A typed, composable Python facade for key-value storage. Decouples domain schemas from persistence backends with orthogonal scope partitioning and layered wrappers (fallback, mirror, tiering). Features an abstract multi-indexing layer—attach custom exact, sparse, dense, or semantic vector indices per store for unified search and hydration."
readme = "README.md"
license = { text = "Apache-2.0" }
license-files = ["LICENSE"]
requires-python = ">=3.12"
authors = [
  { name = "Ahmed Mahran" },
  { email = "ahmed.mahran@mashin.io" },
]
maintainers = [
  { name = "Mashin IO" },
  { email = "ahmed.mahran@mashin.io" },
]
keywords = [
  "kv",
  "key-value",
  "store",
  "cache",
  "indexing",
  "vector-search",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Software Development :: Libraries",
  "Typing :: Typed",
]
dependencies = []

[project.optional-dependencies]
pydantic = ["pydantic>=2"]
redis = ["redis>=5"]
s3 = ["boto3>=1.34"]
gcs = ["google-cloud-storage>=2.14"]
azure = ["azure-storage-blob>=12"]
chroma = ["chromadb>=0.5"]
zstd = ["zstandard>=0.22"]
lz4 = ["lz4>=4.3"]
all = [
  "kvtsjl[pydantic,redis,s3,gcs,azure,chroma,zstd,lz4]",
]
dev = [
  "pytest>=8",
  "pytest-cov>=5",
  "ruff>=0.6",
  "pyright>=1.1",
  "fakeredis>=2.26",
  "moto[s3]>=5.0",
  "freezegun>=1.5",
  "boto3-stubs[s3]>=1.34",
  "chromadb>=0.5",
  "kvtsjl[all]",
]

[project.urls]
Homepage = "https://github.com/mashin-io/kvtsjl"
Repository = "https://github.com/mashin-io/kvtsjl"
Issues = "https://github.com/mashin-io/kvtsjl/issues"
Organization = "https://mashin.io"

[tool.hatch.version]
path = "src/kvtsjl/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/kvtsjl"]

[tool.hatch.build.targets.wheel.sources]
"src/kvtsjl" = "kvtsjl"

[tool.hatch.build.targets.sdist]
include = [
  "/src",
  "/tests",
  "/docs",
  "/README.md",
  "/LICENSE",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = ["-ra", "--strict-markers"]
markers = [
  "s3: tests using moto-simulated S3",
  "redis: tests using fakeredis",
  "gcs: tests using an in-memory fake GCS bucket",
  "azure: tests using an in-memory fake Azure Blob container",
  "chroma: tests using ephemeral ChromaDB",
]

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["I"]
fixable = ["I"]

[tool.ruff.lint.isort]
known-first-party = ["kvtsjl"]
combine-as-imports = true
force-sort-within-sections = true

[tool.pyright]
include = ["src/kvtsjl"]
pythonVersion = "3.12"
typeCheckingMode = "strict"
venvPath = "."
venv = ".venv"
extraPaths = ["src"]
# google-cloud-storage / azure-storage-blob / some redis stubs leave Unknowns;
# keep value checks strict.
reportMissingTypeStubs = false
reportUnknownMemberType = false
reportUnknownVariableType = false
reportUnknownArgumentType = false
reportUnknownLambdaType = false
reportUnnecessaryIsInstance = false
reportUnnecessaryComparison = false
reportPrivateUsage = false
